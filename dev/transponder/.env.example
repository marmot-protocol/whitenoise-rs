# Local-only Transponder environment.
# `just docker-up` will create `dev/transponder/.env` from this template and
# fill in a fresh random server private key when the file is missing.

TRANSPONDER_SERVER_PRIVATE_KEY=
